export interface KeyFeature {
  imgSrc: string;
  title: string;
  description: string;
}

export interface Service {
  headerImg: string;
  title: string;
  description: string;
  keyFeatures: KeyFeature[];
}
